apiVersion: v1
kind: ConfigMap
metadata:
  name: x509-cert-expiry-alert
  namespace: monitoring
  labels:
    grafana_alert: "1"
data:
  rules.yaml: |
    apiVersion: 1
    deleteRules:
      - orgId: 1
        uid: ssl_cert_expiry_days
      - orgId: 1
        uid: ssl_cert_expiry_days_30d
      - orgId: 1
        uid: ssl_cert_expiry_days_v2
      - orgId: 1
        uid: ssl_cert_expiry_days_7d
    groups:
      - orgId: 1
        name: ssl_cert_expiry
        folder: SSL Certificates
        interval: 5m
        rules:
          - uid: ssl_cert_expiry_30d_v2
            title: SSL Certificate Expiring Soon (30d)
            condition: B
            for: 5m
            noDataState: NoData
            execErrState: Alerting
            annotations:
              summary: "{{ $labels.instance }} certificate expires in under 30 days"
            labels:
              severity: info
            data:
              - refId: A
                datasourceUid: influxdb
                queryType: ""
                relativeTimeRange:
                  from: 600
                  to: 0
                model:
                  refId: A
                  query: |
                    from(bucket: "default")
                      |> range(start: -10m)
                      |> filter(fn: (r) => r._measurement == "x509_cert" and r._field == "expiry")
                      |> filter(fn: (r) => r.telegraf_class == "x509-cert-check")
                      |> keep(columns: ["_time", "_value", "source"])
                      |> rename(columns: {source: "instance"})
                      |> group(columns: ["instance"])
                      |> last()
                      |> map(fn: (r) => ({ r with _value: float(v: int(v: float(v: r._value) / 86400.0)) }))
              - refId: B
                datasourceUid: "__expr__"
                queryType: ""
                relativeTimeRange:
                  from: 600
                  to: 0
                model:
                  refId: B
                  type: threshold
                  expression: A
                  conditions:
                    - evaluator:
                        type: lt
                        params: [30]
          - uid: ssl_cert_expiry_14d_v2
            title: SSL Certificate Expiring Soon (14d)
            condition: B
            for: 5m
            noDataState: NoData
            execErrState: Alerting
            annotations:
              summary: "{{ $labels.instance }} certificate expires in under 14 days"
            labels:
              severity: warning
            data:
              - refId: A
                datasourceUid: influxdb
                queryType: ""
                relativeTimeRange:
                  from: 600
                  to: 0
                model:
                  refId: A
                  query: |
                    from(bucket: "default")
                      |> range(start: -10m)
                      |> filter(fn: (r) => r._measurement == "x509_cert" and r._field == "expiry")
                      |> filter(fn: (r) => r.telegraf_class == "x509-cert-check")
                      |> keep(columns: ["_time", "_value", "source"])
                      |> rename(columns: {source: "instance"})
                      |> group(columns: ["instance"])
                      |> last()
                      |> map(fn: (r) => ({ r with _value: float(v: int(v: float(v: r._value) / 86400.0)) }))
              - refId: B
                datasourceUid: "__expr__"
                queryType: ""
                relativeTimeRange:
                  from: 600
                  to: 0
                model:
                  refId: B
                  type: threshold
                  expression: A
                  conditions:
                    - evaluator:
                        type: lt
                        params: [14]
          - uid: ssl_cert_expiry_7d_v2
            title: SSL Certificate Expiring Soon (7d)
            condition: B
            for: 5m
            noDataState: NoData
            execErrState: Alerting
            annotations:
              summary: "{{ $labels.instance }} certificate expires in under 7 days"
            labels:
              severity: critical
            data:
              - refId: A
                datasourceUid: influxdb
                queryType: ""
                relativeTimeRange:
                  from: 600
                  to: 0
                model:
                  refId: A
                  query: |
                    from(bucket: "default")
                      |> range(start: -10m)
                      |> filter(fn: (r) => r._measurement == "x509_cert" and r._field == "expiry")
                      |> filter(fn: (r) => r.telegraf_class == "x509-cert-check")
                      |> keep(columns: ["_time", "_value", "source"])
                      |> rename(columns: {source: "instance"})
                      |> group(columns: ["instance"])
                      |> last()
                      |> map(fn: (r) => ({ r with _value: float(v: int(v: float(v: r._value) / 86400.0)) }))
              - refId: B
                datasourceUid: "__expr__"
                queryType: ""
                relativeTimeRange:
                  from: 600
                  to: 0
                model:
                  refId: B
                  type: threshold
                  expression: A
                  conditions:
                    - evaluator:
                        type: lt
                        params: [7]
