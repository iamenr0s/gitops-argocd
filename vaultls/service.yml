apiVersion: v1
kind: Service
metadata:
  name: vaultls
  namespace: vaultls
  labels:
    app: vaultls
spec:
  type: ClusterIP
  selector:
    app: vaultls
  ports:
    - name: http
      port: 5173
      targetPort: 5173
